# 한국 학술지 참고문헌 검증 서비스 제품 요구사항

> 문서 상태: 구현 기준선  
> 역할: 제품 요구사항과 사용자 행동의 Source of Truth  
> 대상 릴리스: MVP

## 1. 제품 개요

이 제품은 한국도서관·정보학회 편집자가 HWP 또는 HWPX 원고의 본문 인용과 참고문헌을 대조하고, 적용 가능한 인용 규칙을 확인하며, 저자에게 전달할 수정 요청 메모를 작성하도록 돕는 로그인 없는 반응형 웹 서비스다.

제품은 편집자의 판단을 대체하지 않는다. 결정론적으로 확인 가능한 위반과 문맥 검토가 필요한 후보를 구분하고, 모든 결과에 원문 위치와 규정 근거를 연결한다. 편집자는 각 결과를 승인·수정·제외한 뒤 HWP 원고에 붙여 넣을 메모를 항목별로 복사한다.

### 대상 사용자

- 1차 사용자: 연 4회 발간 업무를 수행하는 한국도서관·정보학회 편집자
- 사용 환경: 데스크톱 중심이지만 태블릿과 모바일에서도 결과를 읽고 조작할 수 있는 브라우저
- 전제: 사용자는 최종 편집 판단 권한을 가지며, 서비스 결과를 검토한 후 저자에게 전달한다.

## 2. 생산성 문제와 효과 가설

편집자는 본문과 참고문헌을 반복해서 오가며 누락·불일치·표기 위반을 찾고, 같은 내용을 저자가 이해할 수 있는 짧은 메모로 다시 작성한다. 실제 HWP 원고 10편에서 추출한 편집 메모 387개의 집계는 문장부호·띄어쓰기·오타 102개, URL·DOI·링크 45개, 저자 표기 43개, 본문 인용·복합인용 36개, 자료유형·서지요소 24개였다. 이 수치는 메모 분포이며 실제 오류 빈도와 동일하지 않다. 원고·메모 원문·식별정보는 제품 문서나 공개 저장소에 포함하지 않는다.

**효과 가설:** 규칙 기반 대조, 근거가 제한된 AI 문맥 해석, 짧은 메모 초안을 하나의 흐름으로 제공하면 기존 방식 대비 편집 검토 시간 중앙값을 30% 이상, 수정 요청서 작성 시간 중앙값을 50% 이상 줄일 수 있다.

## 3. 규정 적용 원칙

판정 우선순위는 다음과 같다.

1. 문편협 2024-06-17 최신 공통기준
2. 공통기준에 없는 사항만 APA 7판
3. 한국도서관·정보학회 최신 투고규정의 추가 규칙

과거 오류 안내문과 편집 메모는 빈도 및 업무 흐름의 근거일 뿐 공식 정답이 아니다. 확정 판정에는 버전이 고정된 공식 문서의 조항 번호를 기록한다. 원문에 조항 번호가 없으면 문서명·공개일·쪽·절 제목을 안정 식별자로 사용한다. 이 위치 정보가 없는 규칙은 `오류`를 생성할 수 없으며 `확인 필요`로만 제시한다.

## 4. 핵심 사용자 시나리오

1. 편집자가 공개 URL에 로그인 없이 접속한다.
2. 화면에서 HWP·HWPX, 1회 1파일, 최대 30쪽, 파일당 30MB 제한을 확인한다.
3. 드래그 앤 드롭 또는 `파일 선택` 버튼으로 원고를 업로드한다.
4. 서비스가 파일을 검증하고 텍스트·인용·참고문헌·위치를 추출한다.
5. 서비스가 결정론적 규칙 검사와 제한된 AI 보조 분석을 실행하며 진행 상황을 스트리밍한다.
6. 편집자가 `누락 / 불일치 / 형식수정 / 확인 필요 / 정상` 섹션별 결과를 검토한다.
7. 편집자가 각 항목을 승인·수정·제외하고, 승인한 항목의 메모 문구를 하나씩 복사해 HWP 메모 칸에 붙여 넣는다.
8. 필요하면 카테고리 전체 또는 최종 결과 전체를 복사하거나 다운로드한다.

## 5. 기능 요구사항

### FR-01 로그인 없는 공개 접근

- **설명:** 계정 생성이나 인증 없이 핵심 검사 흐름을 제공한다.
- **사용자 스토리:** 편집자로서 별도 가입 없이 즉시 원고를 검사하고 싶다.
- **수용 기준**
  - **Given** 새 브라우저 세션에서 공개 URL에 접속했을 때, **When** 첫 화면이 열리면, **Then** 로그인 요구 없이 업로드 영역과 지원 제한이 표시된다.
  - **Given** 인증 쿠키가 없을 때, **When** 전체 핵심 흐름을 수행하면, **Then** 업로드부터 결과 복사·다운로드까지 완료할 수 있다.

### FR-02 두 가지 파일 업로드 방식

- **설명:** 드래그 앤 드롭 영역과 `파일 선택` 버튼을 모두 제공하며 하나의 검증 로직을 사용한다.
- **사용자 스토리:** 편집자로서 익숙한 방식으로 원고를 빠르게 선택하고 싶다.
- **수용 기준**
  - **Given** 유효한 HWP/HWPX 1개가 30MB 이하일 때, **When** 드롭하거나 파일 탐색기에서 선택하면, **Then** 동일한 확장자·크기·개수 검증 후 같은 업로드 상태로 이동한다.
  - **Given** 드래그 가능한 파일이 업로드 영역 위에 있을 때, **When** `dragenter`/`dragover`가 발생하면, **Then** 색상 외 테두리·문구 변화로 드롭 가능 상태를 표시한다.
  - **Given** 키보드 사용자일 때, **When** `파일 선택` 버튼에 포커스를 옮겨 Enter 또는 Space를 누르면, **Then** 파일 탐색기가 열린다.
  - **Given** 지원하지 않는 파일을 드롭했을 때, **When** 검증이 끝나면, **Then** “HWP 또는 HWPX 파일 1개만 업로드할 수 있습니다”와 거부 이유를 인라인 및 접근성 라이브 영역에 표시한다.

### FR-03 입력 검증

- **설명:** 공식 지원 형식과 운영 제한을 업로드 전에 표시하고 서버에서 재검증한다.
- **사용자 스토리:** 편집자로서 처리할 수 없는 파일인 이유를 즉시 알고 싶다.
- **수용 기준**
  - **Given** DOC, DOCX, PDF, 실행 파일, 확장자 위장 파일, 2개 이상 파일 또는 30MB 초과 파일일 때, **When** 업로드를 시도하면, **Then** 처리하지 않고 해당 제한을 구체적으로 표시한다.
  - **Given** 암호화·손상·지원하지 않는 HWP 버전 또는 30쪽 초과 원고일 때, **When** 서버 검증 또는 추출을 수행하면, **Then** 실패 단계와 재시도 가능한 조치를 표시하며 검사 성공 상태를 반환하지 않는다.
  - **Given** HWP/HWPX 1개가 모든 제한을 만족할 때, **When** 검증이 완료되면, **Then** 세션 ID를 만들고 추출을 시작한다.

### FR-04 문서 구조와 원문 위치 추출

- **설명:** 본문, 본문 인용, 국문·영문 참고문헌 및 원문 위치를 구조화한다.
- **사용자 스토리:** 편집자로서 결과를 원고의 정확한 위치에서 확인하고 싶다.
- **수용 기준**
  - **Given** 정상 HWP/HWPX 원고일 때, **When** 추출이 끝나면, **Then** 각 항목에 섹션·문단 인덱스와 가능한 경우 참고문헌 번호를 부여한다.
  - **Given** 위치를 안정적으로 특정할 수 없을 때, **When** 결과를 만들면, **Then** 위치를 추측하지 않고 `확인 필요`로 표시한다.

### FR-05 인용·참고문헌 대조와 규칙 검사

- **설명:** 확정된 규칙을 우선 결정론적으로 검사하고 문맥 항목만 AI가 보조한다.
- **사용자 스토리:** 편집자로서 반복 대조를 자동화하되 근거 없는 판정을 피하고 싶다.
- **수용 기준**
  - **Given** 추출 데이터와 버전 고정 규칙 레지스트리가 있을 때, **When** 검사가 실행되면, **Then** 규칙별 결과에 `rule_id`, 공식 근거 위치, 판정 방식과 확신도를 기록한다.
  - **Given** 규칙 근거 또는 문맥이 불충분할 때, **When** 판정을 생성하면, **Then** `오류`로 확정하지 않고 `확인 필요`로 분류한다.
  - **Given** 하이퍼링크가 존재한다는 사실만 확인될 때, **When** 검사하면, **Then** 이를 오류나 삭제 요청으로 만들지 않는다.

### FR-06 스트리밍 진행과 취소

- **설명:** 추출·대조·규칙 검사·AI 보조·결과 종합 진행을 실시간 표시한다.
- **사용자 스토리:** 편집자로서 긴 처리 중 현재 단계와 실패 여부를 알고 싶다.
- **수용 기준**
  - **Given** 검사가 실행 중일 때, **When** 단계 또는 진행률이 바뀌면, **Then** SSE로 상태를 갱신하고 스크린 리더 라이브 영역에도 과도하지 않게 알린다.
  - **Given** 사용자가 취소를 확인했을 때, **When** 취소를 실행하면, **Then** 신규 작업을 중단하고 임시 파일 삭제를 예약하며 취소 상태를 표시한다.
  - **Given** 스트림이 끊겼을 때, **When** 브라우저가 마지막 이벤트 ID로 재연결하면, **Then** 중복 없이 남은 이벤트 또는 최종 상태를 받는다.

### FR-07 카테고리별 결과 화면

- **설명:** 결과를 필수 5개 카테고리로 그룹화하고 원문 등장 순서로 정렬한다.
- **사용자 스토리:** 편집자로서 같은 종류의 문제를 묶어 처리하고 싶다.
- **수용 기준**
  - **Given** 검사 결과가 있을 때, **When** 결과 화면을 열면, **Then** `누락 / 불일치 / 형식수정 / 확인 필요 / 정상` 섹션과 각 건수 배지를 표시한다.
  - **Given** 카테고리 필터를 선택하거나 섹션 제목을 누를 때, **When** 상태가 바뀌면, **Then** 해당 섹션을 필터링하거나 접기·펼치며 키보드와 `aria-expanded`를 지원한다.
  - **Given** 개별 결과 항목일 때, **When** 표시되면, **Then** 원문 위치, 발견 내용, 수정 요청 문구, 근거 규정, 심각도, 확신도, AI 보조 여부를 포함한다.

### FR-08 결과 승인·수정·제외

- **설명:** 편집자가 모든 제안을 최종 통제한다.
- **사용자 스토리:** 편집자로서 자동 결과를 검토해 전달 여부와 문구를 직접 결정하고 싶다.
- **수용 기준**
  - **Given** 결과 항목이 있을 때, **When** 승인·수정·제외 중 하나를 선택하면, **Then** 항목 상태와 카테고리 건수를 즉시 갱신한다.
  - **Given** 메모 문구를 수정할 때, **When** 저장하면, **Then** 원래 AI 초안과 편집본을 구분하되 원고 내용은 로그에 기록하지 않는다.
  - **Given** `확인 필요` 항목일 때, **When** 편집자가 검토하지 않았으면, **Then** 최종 복사·다운로드 전에 미검토 상태를 알린다.

### FR-09 항목별 메모 복사

- **설명:** 각 결과 카드의 복사 아이콘은 `memo_text` 순수 텍스트만 클립보드로 복사한다. 이 기능이 결과 처리의 1차 행동이다.
- **사용자 스토리:** 편집자로서 위에서부터 HWP 메모를 하나씩 달면서 진행 상황을 추적하고 싶다.
- **수용 기준**
  - **Given** 결과 항목이 승인 상태일 때, **When** 복사 버튼을 누르면, **Then** 번호·아이콘·마크다운·UI 라벨 없이 해당 항목의 `memo_text` 한 덩어리만 복사한다.
  - **Given** 복사가 성공했을 때, **When** 완료 이벤트가 발생하면, **Then** 버튼을 최소 1.5초 동안 체크 표시와 “복사됨” 접근성 라벨로 바꾸고 항목의 복사 완료 상태를 유지한다.
  - **Given** 이미 복사한 항목일 때, **When** 결과 목록을 계속 탐색하면, **Then** 색상 외 아이콘과 텍스트로 복사 완료를 식별할 수 있다.
  - **Given** Clipboard API가 거부되거나 실패했을 때, **When** 복사를 시도하면, **Then** 선택 가능한 순수 텍스트 영역과 실패 안내를 노출하고 항목을 복사 완료로 표시하지 않는다.

### FR-10 카테고리·전체 복사와 다운로드

- **설명:** 항목별 복사를 보조하는 일괄 출력 기능을 제공한다.
- **사용자 스토리:** 편집자로서 필요할 때 한 카테고리나 승인 결과 전체를 별도 문서로 전달하고 싶다.
- **수용 기준**
  - **Given** 승인 항목이 있을 때, **When** 카테고리 전체 복사 또는 전체 결과 복사를 누르면, **Then** 승인 항목의 `memo_text`만 원문 순서대로 빈 줄 하나로 구분해 복사한다.
  - **Given** 최종 목록을 다운로드할 때, **When** 다운로드를 실행하면, **Then** 원문 위치·카테고리·메모·근거가 포함된 UTF-8 파일을 생성하고 제외 항목은 기본적으로 포함하지 않는다.

### FR-11 짧고 근거 있는 메모 생성

- **설명:** 저자가 바로 수정할 수 있는 짧은 한국어 한두 문장과 근거 위치를 만든다.
- **사용자 스토리:** 편집자로서 HWP 메모 칸에 그대로 붙일 수 있는 문구가 필요하다.
- **수용 기준**
  - **Given** 검사 결과와 공식 근거가 있을 때, **When** 메모를 생성하면, **Then** 기본 180자 이하의 순수 텍스트로 행동 요청과 `{규정명} {조항번호 또는 안정 위치}, {rule_id}`를 포함한다.
  - **Given** 공식 근거 위치가 없을 때, **When** 메모를 생성하면, **Then** 단정 문구를 쓰지 않고 확인 요청으로 표현한다.
  - **Given** `정상` 항목일 때, **When** 표시하면, **Then** 수정 요청을 만들지 않고 통과 근거만 제공한다.

### FR-12 자동 삭제와 세션 종료

- **설명:** 업로드 파일과 파생 데이터를 최소 기간만 보관한다.
- **사용자 스토리:** 편집자로서 원고가 서비스에 남지 않는다는 확신을 갖고 싶다.
- **수용 기준**
  - **Given** 추출이 끝났거나 실패·취소했을 때, **When** 원본 파일이 더 이상 필요 없으면, **Then** 즉시 삭제하고 어떠한 경우에도 업로드 후 10분을 넘겨 보관하지 않는다.
  - **Given** 결과 검토 세션이 종료되지 않았을 때, **When** 업로드 후 2시간이 지나면, **Then** 추출 데이터·결과·Copilot 세션 상태를 삭제하고 만료 화면을 표시한다.
  - **Given** 삭제 작업이 실패했을 때, **When** 재시도 한도를 넘기면, **Then** 원고 내용 없이 운영 경보를 발생시키며 성공으로 위장하지 않는다.

## 6. 결과 분류 체계

| 카테고리 | 정의 | 대표 항목 |
|---|---|---|
| 누락 | 있어야 할 인용·참고문헌·서지요소가 없음 | 본문에만 있는 인용, 참고문헌에만 있는 항목, 영문화 목록 누락, DOI 누락 |
| 불일치 | 대응 항목은 있으나 핵심 값이 다름 | 저자명, 발행연도, 제목, 국·영문 대응 불일치 |
| 형식수정 | 값은 식별되나 표기·배열 규정을 위반함 | 문장부호, 띄어쓰기, 이탤릭, 권·호·쪽, `출처: URL`, 복합 인용 순서 |
| 확인 필요 | 추출·근거·문맥 부족으로 자동 확정할 수 없음 | 모호한 저자 대응, 외부 확인 실패, `and/&`, `와/과` 문맥 판단 |
| 정상 | 활성화된 검사 규칙을 통과함 | 일치 확인된 인용·참고문헌 및 통과 항목 |

카테고리와 심각도는 별도 축이다. 예를 들어 DOI 누락은 규정 적용이 명확하면 `누락+오류`, 자료유형 확인이 필요하면 `누락+경고`가 될 수 있다. 자동 판정 불가 결과는 항상 `확인 필요` 카테고리이며 심각도도 `확인 필요`다.

## 7. MVP 검사 규칙

메모 템플릿은 `{사람이 읽는 원문 위치}: {수정 행동}. (근거: {규정명} {검증된 조항번호 또는 쪽·절})`이다. `rule_id`와 UUID는 내부 추적에만 사용하고 저자에게 복사하는 메모에는 넣지 않는다. 공통기준의 문서명은 `인용 및 참고문헌의 기술요소와 형식에 관한 공통기준`, 버전은 `2024-06-17 개정`, 검증일은 `2026-08-22`로 고정한다. CR-06은 Ⅱ-9)가 영문 변환 형식만 규정하고 병기 의무를 직접 규정하지 않으므로, 학회 투고규정 조항 확인 전까지 경고 후보로만 취급하며 확정 오류를 만들지 않는다.

| 규칙 ID | 카테고리 | 검사 대상 | 근거 규정 | 심각도 | 자동 판정 | 저자용 메모 예시 |
|---|---|---|---|---|---|---|
| CR-01 | 누락 | 본문 인용이 참고문헌에 없음 | 공통기준 `Ⅱ-1)-(1)` | 오류 | 예 | 본문 인용에 대응하는 참고문헌을 추가해 주세요. (근거: 인용 및 참고문헌의 기술요소와 형식에 관한 공통기준 Ⅱ-1)-(1), CR-01) |
| CR-02 | 누락 | 참고문헌만 있고 본문 인용 없음 | 공통기준 `Ⅱ-1)-(1)` | 경고 | 예 | 이 참고문헌의 본문 인용 위치를 확인해 주세요. 인용하지 않았다면 목록에서 제외해 주세요. (근거: 인용 및 참고문헌의 기술요소와 형식에 관한 공통기준 Ⅱ-1)-(1), CR-02) |
| CR-03 | 불일치 | 저자명·발행연도·제목 대응 | 공통기준 `Ⅱ-1)-(1)` | 오류 | 정규화 일치만 예 | 본문 인용과 참고문헌의 저자명·연도·제목이 일치하도록 수정해 주세요. (근거: 인용 및 참고문헌의 기술요소와 형식에 관한 공통기준 Ⅱ-1)-(1), CR-03) |
| CR-04 | 형식수정 | 서로 다른 저자의 복합 인용 | 공통기준 `Ⅰ-9)` | 오류 | 예 | 서로 다른 저자의 복합 인용을 참고문헌 배열순으로 정렬해 주세요. (근거: 인용 및 참고문헌의 기술요소와 형식에 관한 공통기준 Ⅰ-9), CR-04) |
| CR-05 | 형식수정 | 동일 저자의 복합 인용 | 공통기준 `Ⅰ-7)` | 오류 | 예 | 동일 저자의 복합 인용을 연대순으로 정렬해 주세요. (근거: 인용 및 참고문헌의 기술요소와 형식에 관한 공통기준 Ⅰ-7), CR-05) |
| CR-06 | 누락 | 국문 참고문헌의 영문화 목록 병기 후보 | 공통기준 `Ⅱ-9)`은 변환 형식만 확인됨 | 경고 | 아니요 | 학회 투고규정의 병기 의무를 확인하고 국문 참고문헌의 영문화 목록을 검토해 주세요. (근거: 인용 및 참고문헌의 기술요소와 형식에 관한 공통기준 Ⅱ-9), CR-06) |
| CR-07 | 형식수정 | 한국 저자 영문명과 영문 목록 배열 | 공통기준 `Ⅱ-9) + Ⅱ-10)` | 오류 | 사전 대응 시 예 | 한국 저자 이름을 이니셜이 아닌 전체 영문명으로 표기하고 영문명 알파벳순으로 재배열해 주세요. (근거: 인용 및 참고문헌의 기술요소와 형식에 관한 공통기준 Ⅱ-9) + Ⅱ-10), CR-07) |
| CR-08 | 형식수정 | 연속간행물 논문명 문장식 표기와 단행본 서명·간행물명 제목식 표기 | 공통기준 `Ⅱ-1)-(5)`; 콜론 뒤 첫 단어는 APA 7 보조 경고 | 오류(보조 규칙은 경고) | 예 | 자료유형에 맞는 대문자 표기를 적용해 주세요. (근거: 인용 및 참고문헌의 기술요소와 형식에 관한 공통기준 Ⅱ-1)-(5), CR-08) |
| CR-09 | 형식수정 | 출처 URL 표기 | 공통기준 `Ⅱ-6)-(1)` | 오류 | 예 | 출처 표기를 `출처: URL` 형식으로 수정해 주세요. (근거: 인용 및 참고문헌의 기술요소와 형식에 관한 공통기준 Ⅱ-6)-(1), CR-09) |
| CR-10 | 누락 | 학술지 논문의 확인 가능한 DOI | 공통기준 `Ⅱ-3)-(1)` | 경고 | DOI 존재 확인 시 예 | DOI가 확인되는 학술지 논문이면 DOI를 추가해 주세요. (근거: 인용 및 참고문헌의 기술요소와 형식에 관한 공통기준 Ⅱ-3)-(1), CR-10) |
| CR-11 | 형식수정 | 서양 참고문헌의 복수 저자 연결 | 공통기준 `Ⅱ-1)-(4)` | 오류 | 예 | 서양 참고문헌의 마지막 저자 앞에 앤드기호(&)를 표기해 주세요. (근거: 인용 및 참고문헌의 기술요소와 형식에 관한 공통기준 Ⅱ-1)-(4), CR-11) |
| CR-12 | 정상 | 활성 규칙을 통과한 대응 항목 | 적용된 규칙 집합 | 정보 | 예 | 수정 요청 없음 |
| CR-13 | 형식수정 | 국내문헌 본문 인용의 공저자 구분자 | 공통기준 `Ⅰ-6)` | 오류 | 예 | 본문 인용의 공저자는 쉼표로 구분해 주세요. (근거: 인용 및 참고문헌의 기술요소와 형식에 관한 공통기준 Ⅰ-6)) |
| CR-14 | 형식수정 | 국문 참고문헌 저자명 뒤 불필요한 온점 | 공통기준 `Ⅱ-2)-(1)`, `Ⅱ-3)-(1)`, `Ⅱ-4)-(1)`, `Ⅱ-6)-(1)` 중 자료유형별 적용 | 오류 | 예 | 국문 저자명 뒤의 온점을 삭제해 주세요. (근거: 자료유형별 공통기준 조항) |
| CR-15 | 형식수정 | 국내문헌·서양문헌·동양문헌 구간 순서 | 공통기준 `Ⅱ-1)-(1)` | 오류 | 구간 확정 시 예 | 국내문헌 → 서양문헌 → 동양문헌 순으로 배열해 주세요. |
| CR-16 | 형식수정 | 국문 저자명 가나다순 | 공통기준 `Ⅱ-1)-(2)` | 오류 | 예 | 저자명 가나다순으로 배열해 주세요. |
| CR-17 | 형식수정 | 동일 저자 문헌의 연대순 | 공통기준 `Ⅱ-1)-(2)` | 오류 | 예 | 동일 저자의 문헌을 오래된 출판연도순으로 배열해 주세요. |
| CR-18 | 형식수정 | 동일 저자·동일 연도 a, b, c 구분 | 공통기준 `Ⅱ-3)-(2)` | 오류 | 예 | 발행연도 뒤에 a, b, c를 붙여 구분해 주세요. |
| CR-19 | 확인 필요 | 저자 표기만으로 자료군 구분 불확실 | 공통기준 `Ⅱ-1)-(1)` | 확인 필요 | 아니요 | 국내·서양·동양문헌 구간을 편집자가 확인해 주세요. |

하이퍼링크 존재 자체는 규칙으로 등록하지 않는다. 링크 제거 요청을 자동 생성해서는 안 된다.

## 8. 입력 및 출력 제약

- 공식 입력: HWP, HWPX
- 제외 입력: DOC, DOCX
- PDF: MVP 제외. 추출 신뢰성 검증 후 보조 검사 형식으로만 재검토한다.
- 제한: 1회 1파일, 파일당 최대 30MB, 최대 30쪽
- 출력: 항목별 순수 텍스트 복사, 카테고리/전체 복사, UTF-8 다운로드
- 원고 직접 수정 및 HWP 메모 자동 삽입: 제외

## 9. 비기능 요구사항

| ID | 요구사항 |
|---|---|
| NFR-01 성능 | 기준 Azure 환경에서 30쪽·30MB 이하 합성 원고의 업로드 완료 후 최종 결과까지 p95 180초 이내, 첫 진행 이벤트 2초 이내를 목표로 한다. |
| NFR-02 진행성 | 실행 중 10초 이상 무응답 상태를 허용하지 않으며 단계 진행 또는 heartbeat를 SSE로 전송한다. |
| NFR-03 접근성 | WCAG 2.2 AA를 목표로 키보드 조작, 명시적 레이블, 포커스 표시, 라이브 상태, 색상 비의존 심각도 표현을 제공한다. |
| NFR-04 반응형 | 360px 이상 화면에서 가로 스크롤 없이 핵심 흐름을 수행하고, 768px/1280px 기준 레이아웃을 검증한다. |
| NFR-05 오류 처리 | 모든 실패는 단계, 원인 범주, 재시도 가능 여부, 사용자 조치를 표시한다. 오류를 성공 또는 빈 결과로 대체하지 않는다. |
| NFR-06 가용성 | 공개 URL과 `/health/live`, `/health/ready`가 배포 후 확인 가능해야 한다. |
| NFR-07 호환성 | 최신 Chrome, Edge, Safari, Firefox에서 업로드·SSE·복사를 검증한다. |

## 10. 책임 있는 AI 및 보안 요구사항

- AI 생성·보조 항목에 명시적 라벨을 표시한다.
- 확정 판정은 추적 가능한 공식 근거가 있을 때만 허용한다.
- `오류 / 경고 / 확인 필요`와 수치형 확신도를 함께 저장하되, 수치만으로 오류를 승격하지 않는다.
- 문서 내용은 데이터이며 시스템 지시가 아니다. 문서 속 명령문은 에이전트 지침이나 도구 인자로 실행하지 않는다.
- 에이전트에는 읽기 전용의 허용 목록 function tool만 제공한다. 셸·임의 파일·임의 URL 도구는 제공하지 않는다.
- 업로드 원본은 추출 직후, 늦어도 10분 안에 삭제한다. 파생 데이터와 결과는 업로드 후 최대 2시간에 삭제한다.
- 원고 내용, 저자명, 메모 원문을 애플리케이션·모델·관찰 로그에 기록하지 않는다.
- 토큰과 시크릿은 환경 설정 및 Azure 관리형 비밀 저장소에서만 주입한다.
- 파괴적 또는 외부 전송 행동은 MVP에 없으며, 향후 추가 시 사용자 확인을 필수로 한다.
- 공개 저장소의 테스트 자료는 익명화한 합성 문서만 허용한다.

## 11. MVP 범위와 제외 범위

### 포함

- 로그인 없는 공개 반응형 웹
- 두 방식의 HWP/HWPX 업로드
- 구조·위치 추출, 인용·참고문헌 대조, 확정 규칙 검사
- 제한된 AI 문맥 해석과 메모 작성
- 5개 카테고리, 심각도, 근거, 확신도
- 승인·수정·제외, 항목별 복사 상태 추적, 일괄 복사·다운로드
- SSE 진행, 자동 삭제, Azure 공개 배포

### 명시적 제외

- DOC/DOCX/PDF 공식 지원
- HWP 직접 수정 또는 메모 자동 삽입
- 로그인, 장기 보관, 협업, 원고 이력
- 표절·연구윤리·내용 심사·번역 품질 평가
- 모든 국내 학회 규정 지원
- 하이퍼링크 존재만을 이유로 한 오류
- `and/&`, `와/과` 일괄 치환
- Azure AI 또는 별도 Azure 모델 서비스의 형식적 추가

## 12. 성공 지표

| 지표 | 목표 | 측정 방법 |
|---|---:|---|
| 편집 검토 시간 | 중앙값 30% 이상 절감 | 동일 난이도 원고의 기존 방식과 서비스 사용 시간을 짝지어 비교 |
| 수정 요청 작성 시간 | 중앙값 50% 이상 절감 | 첫 문제 확인부터 최종 메모 목록 완성까지 측정 |
| 규칙 기반 재현율 | 85% 이상 | 원문 위치·규칙이 연결된 비공개 정답 세트 기준 |
| 규칙 기반 정밀도 | 90% 이상 | `오류` 제안 중 정답 비율 |
| 편집자 무수정 승인률 | 70% 이상 | 승인 항목 중 `memo_text` 미수정 비율 |
| 제외율 | 15% 이하 | 전체 제안 중 편집자가 제외한 비율 |
| 근거 없는 확정 판정 | 0% | `오류` 결과의 `rule_source/source_locator` 완전성 검사 |
| 개별 복사 완료율 | 승인 항목의 80% 이상 | 세션 내 `copied` 클라이언트 상태 집계; 원고 내용은 수집하지 않음 |
| 처리 성능 | p95 180초 이내 | 30쪽 경계 합성 픽스처의 업로드 후 결과 완료 시간 |

## 13. 릴리스 기준(Definition of Done)

MVP는 다음 조건을 모두 만족해야 릴리스할 수 있다.

1. 모든 FR의 Given/When/Then 자동 또는 수동 수용 테스트가 통과한다.
2. HWP/HWPX 정상·암호화·손상·과대·초과 페이지 합성 픽스처 테스트가 통과한다.
3. CR-01~CR-11의 근거 조항이 편집자 검토를 받으며, 병기 의무가 미확인된 CR-06은 확정 오류로 활성화하지 않는다.
4. 확정 핵심 규칙과 정정 지침의 회귀 테스트가 통과한다.
5. 항목별 순수 텍스트 복사, 실패 대체 경로, 복사 상태 추적, 카테고리/전체 복사가 지원 브라우저에서 동작한다.
6. 업로드·파생 데이터 삭제와 2시간 만료가 자동 테스트로 입증된다.
7. 프롬프트 인젝션, 임의 도구 호출, 로그 유출 테스트가 통과한다.
8. 접근성 자동 검사와 키보드 수동 검사가 통과한다.
9. 30쪽 경계 합성 문서 성능 목표를 충족한다.
10. Azure 인프라가 IaC와 CI/CD로 재현 가능하며 공개 URL이 새 브라우저 세션에서 로그인 없이 동작한다.
11. Application Insights에서 원고 내용 없이 상태·지연·오류를 진단할 수 있다.
12. 제출용 커밋 해시와 공개 URL을 기록하고 전체 E2E 검사를 해당 커밋에서 재실행한다.
